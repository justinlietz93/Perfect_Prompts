# Runtime Bindings

Prompt implementations embedded in application-language source. These trees intentionally preserve their language-specific forms rather than pretending every representation is byte-for-byte equivalent.
