# Prompt Templates

Standalone and specialized prompt-template families live here. These are kept separate from `Standards/` because a template is a reusable instrument for a task class, while a standard governs what must remain true for its scope.

Historical and specialized variants remain visible rather than being silently collapsed into one universal prompt.
