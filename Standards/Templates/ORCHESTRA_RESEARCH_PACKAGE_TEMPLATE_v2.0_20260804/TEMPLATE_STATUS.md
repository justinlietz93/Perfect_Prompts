# TEMPLATE ONLY

This directory is a neutral seed. It contains no scientific claim, accepted result, controlling semantic authority, Guardian verdict, Auditor verdict, or publication-ready artifact.

Create an active package with `tools/init_package.py` rather than renaming this directory manually.
