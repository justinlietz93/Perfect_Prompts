# Software / Tools for {{GROUP_NAME}}

## Default rule

Use the simplest toolchain that supports the actual learning objective. Add specialist tools only when a concrete task requires them.

## Core tools

- `{{TOOL}}` — `{{PURPOSE}}`

## Add later

- `{{TOOL}}` — start when `{{TRIGGER}}`

## Reproducibility / craftsmanship requirements

Record the parameters, versions, inputs, and outputs required to reproduce any computational, laboratory, design, or build result used as evidence of understanding.

If the activity is research-grade, defer to the canonical Research & Evidence Standard.

## Work location

```text
work/{{GROUP_WORK_DIR}}/
```

Generated work does not belong in `resources/`.
