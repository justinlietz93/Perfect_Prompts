# Existing Resource Inventory

Record each supplied resource exactly once unless intentional duplication is documented.

| Group | Resource | Role | Format | Location | SHA-256 / identifier | Notes |
|---|---|---|---|---|---|---|
| `{{GROUP}}` | `{{RESOURCE}}` | `{{ROLE}}` | `{{FORMAT}}` | `{{PATH_OR_URI}}` | `{{HASH_OR_ID}}` | `{{NOTES}}` |

## Duplicate rule

Do not copy the same large source into multiple groups merely because it is relevant to more than one. Assign one canonical location and cross-reference it elsewhere.
