# Resource Acquisition Queue

This queue must follow the same activation logic as the curriculum. It is not a separate recommendation list.

## Acquire first

1. `{{RESOURCE}}` — activates with `{{GROUP}}`
2. `{{RESOURCE}}` — activates with `{{GROUP}}`

## Acquire when later gates approach

1. `{{RESOURCE}}` — obtain when `{{GATE}}`

## Rule

Do not purchase or download the entire advanced library merely because it appears in the curriculum. Acquire a resource before or when its role becomes active, unless an unusually strong cost/availability reason justifies earlier acquisition.
