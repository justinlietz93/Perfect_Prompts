# Current-Work Transfer Map

| Current target | Primary groups | Supporting groups | Exact concept needed | Relationship status | Next verification / application step |
|---|---|---|---|---|---|
| `{{TARGET}}` | `{{PRIMARY}}` | `{{SUPPORTING}}` | `{{CONCEPT}}` | exact / partial / conjectural / analogy / unknown | `{{NEXT}}` |

## Why the curriculum order is shaped this way

For every load-bearing dependency, write the causal reason rather than merely stating that one subject conventionally comes before another.

Example form:

> `{{GROUP_A}}` precedes `{{GROUP_B}}` because `{{SPECIFIC_CAPABILITY}}` is required to distinguish `{{COMMON_CONFUSION}}` before `{{DOWNSTREAM_TASK}}` is attempted.
